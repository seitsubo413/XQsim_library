services:
  xqsim-backend:
    build:
      context: .
      dockerfile: Dockerfile
    platform: linux/amd64
    shm_size: "2gb"
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - XQSIM_TRACE_TIMEOUT_SECONDS=86400  # 24時間（実質無制限）


